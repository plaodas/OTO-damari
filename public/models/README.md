MiDaS v2.1 small weights are from Intel ISL (MIT).
Converted to TensorFlow.js uint8 graph from model-small.pb.

Selfie Segmentation general is MediaPipe / TensorFlow.js (Apache 2.0), vendored from TF Hub.
